let page_klarif = document.getElementById("page_klarif");
let hal_utama = document.getElementById('halaman_utama ');

window.addEventListener('scroll', () => {
    let value = window.scrollY;
    // page_klarif.style.marginTop = value * -1.5 + 'px';
    // hal_utama.style.top = value * 0.5 + 'px';
});